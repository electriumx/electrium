
import { Facebook, Instagram, Linkedin } from "lucide-react";

const SocialButtons = () => {
  return (
    <div className="fixed bottom-4 right-4 flex flex-col gap-2 z-50">
      {/* Empty component - social buttons moved to footer */}
    </div>
  );
};

export default SocialButtons;
